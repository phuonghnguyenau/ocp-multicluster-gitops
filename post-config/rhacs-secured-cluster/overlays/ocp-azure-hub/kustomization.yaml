apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

patches:
  - target:
      kind: SecuredCluster
      name: stackrox-secured-cluster-services
      namespace: stackrox
    path: secured-cluster-patch.yaml
